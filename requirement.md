# CUDA
# CUDNN
# OpenCV
可以直接
```
    sudo apt update
    sudo apt install libopencv-dev
```
# TensorRT-8.6.1.6 源码安装
# PCL
# VTK-7.1
```
sudo apt update

sudo apt install \
    libpcl-dev \
    libvtk7-dev \
    libvtk7-qt-dev
```
# ROS noetic or ROS2 Humble
# realsense 驱动(+ ros 驱动)
# k4a 驱动（+ ros 驱动）
# YOLOv8  
为了适配TensorRT_Pro 需要改动部分源码，修改后的源码已上传github  
https://github.com/L-Anjing/Ultralytics-main
# 其他的依赖缺少报错时再装吧，不记得了
